# Team Templates

No saved team templates yet. `team-workflow` assembles a team for each concrete task without requiring one.

When a workflow has proved reusable and the user asks to keep it, add `teams/<name>/team.json` and `TEAM.md` here.

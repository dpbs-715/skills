# UI designer skill sources

- `visual-design-foundations/` was copied from the user's local `skillsCollection` on 2026-09-24. The source package did not include a license file.
- Local corrections: fixed WCAG large-text size thresholds, distinguished normal and large text at AAA, and corrected the white-on-blue contrast example.
- `frontend-design/` was copied from the user's `.cc-switch/skills/frontend-design` on 2026-09-24. Its Apache 2.0 license is retained in `frontend-design/LICENSE.txt`.

These packages are used only by the `ui-designer` role through `agentOnlySkills` in `meta.ts`.
